} else {
return(x)
}
})
str(encoded_data)
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("~", paste(colnames(encoded_data), collapse = " + ")))
n_classes <- 3
lca_result <- poLCA(lca_formula, data = encoded_data, nclass = n_classes, na.rm = TRUE)
clear
clean
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("~", paste(colnames(encoded_data), collapse = " + ")))
n_classes <- 3
print(lca_formula)
# Set seed for reproducibility
set.seed(123)
# Generate sample data: 5 binary variables (0, 1)
n_samples <- 1000
data <- data.frame(
var1 = sample(0:1, n_samples, replace = TRUE),
var2 = sample(0:1, n_samples, replace = TRUE),
var3 = sample(0:1, n_samples, replace = TRUE),
var4 = sample(0:1, n_samples, replace = TRUE),
var5 = sample(0:1, n_samples, replace = TRUE)
)
# Define the LCA formula: all variables in data
lca_formula <- as.formula(cbind(var1, var2, var3, var4, var5) ~ 1)
# Run Latent Class Analysis with 2 classes
lca_result <- poLCA(lca_formula, data = data, nclass = 2, na.rm = FALSE, nrep = 30, maxiter = 3000)
# View LCA results
print(lca_result)
# Show class memberships
lca_result$predclass
# Show probabilities of item-response for each class
lca_result$probs
View(data)
#TEST
# Set seed for reproducibility
set.seed(123)
# Generate sample data: 5 binary variables (0, 1)
n_samples <- 1000
data <- data.frame(
var1 = sample(0:1, n_samples, replace = TRUE),
var2 = sample(0:1, n_samples, replace = TRUE),
var3 = sample(0:1, n_samples, replace = TRUE),
var4 = sample(0:1, n_samples, replace = TRUE),
var5 = sample(0:1, n_samples, replace = TRUE)
)
# Run Latent Class Analysis with 2 classes
lca_formula <- as.formula(cbind(var1, var2, var3, var4, var5) ~ 1)
lca_result <- poLCA(lca_formula, data = data, nclass = 2, na.rm = FALSE, nrep = 30, maxiter = 3000)
# View LCA results
print(lca_result)
# Show class memberships (powinny pokazać przypisanie klas)
print(lca_result$predclass)
# Show probabilities of item-response for each class (powinny pokazać prawdopodobieństwa dla każdej zmiennej)
print(lca_result$probs)
install.packages("poLCA")
library(dplyr)
library(here)
#install.packages("poLCA")
library(poLCA)
#TEST
# Set seed for reproducibility
set.seed(123)
# Generate sample data: 5 binary variables (0, 1)
n_samples <- 1000
data <- data.frame(
var1 = sample(0:1, n_samples, replace = TRUE),
var2 = sample(0:1, n_samples, replace = TRUE),
var3 = sample(0:1, n_samples, replace = TRUE),
var4 = sample(0:1, n_samples, replace = TRUE),
var5 = sample(0:1, n_samples, replace = TRUE)
)
# Run Latent Class Analysis with 2 classes
lca_formula <- as.formula(cbind(var1, var2, var3, var4, var5) ~ 1)
lca_result <- poLCA(lca_formula, data = data, nclass = 2, na.rm = FALSE, nrep = 30, maxiter = 3000)
# View LCA results
print(lca_result)
# Show class memberships (powinny pokazać przypisanie klas)
print(lca_result$predclass)
# Show probabilities of item-response for each class (powinny pokazać prawdopodobieństwa dla każdej zmiennej)
print(lca_result$probs)
#TEST
# Set seed for reproducibility
set.seed(123)
# Generate sample data: 5 binary variables (0, 1)
n_samples <- 1000
data <- data.frame(
var1 = sample(0:1, n_samples, replace = TRUE),
var2 = sample(0:1, n_samples, replace = TRUE),
var3 = sample(0:1, n_samples, replace = TRUE),
var4 = sample(0:1, n_samples, replace = TRUE),
var5 = sample(0:1, n_samples, replace = TRUE)
)
# Recode the data to start from 1
data <- data + 1
# Run Latent Class Analysis with 2 classes
lca_formula <- as.formula(cbind(var1, var2, var3, var4, var5) ~ 1)
lca_result <- poLCA(lca_formula, data = data, nclass = 2, na.rm = FALSE, nrep = 30, maxiter = 3000)
# View LCA results
print(lca_result)
# Show class memberships (powinny pokazać przypisanie klas)
print(lca_result$predclass)
# Show probabilities of item-response for each class (powinny pokazać prawdopodobieństwa dla każdej zmiennej)
print(lca_result$probs)
library(dplyr)
library(here)
#install.packages("poLCA")
library(poLCA)
# Define the path to the CSV file
data_path <- normalizePath(file.path(here(), "..", "..", "data", "encoded", "encoded_data.csv"))
# Read the CSV file into a data frame
encoded_data <- read.csv(data_path, header = TRUE, sep = ",", stringsAsFactors = FALSE)
encoded_data[] <- lapply(encoded_data, function(x) {
if (all(x %in% c("True", "False"))) {  # Check if the column contains only "True" or "False"
return(as.integer(x == "True"))  # Convert "True" to 1, "False" to 0
} else {
return(x)  # Leave other columns unchanged
}
})
View(encoded_data)
# Recode the data to start from 1
encoded_data <- encoded_data + 1
View(encoded_data)
str(encoded_data)
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("~", paste(colnames(encoded_data), collapse = " + ")))
n_classes <- 2
lca_result <- poLCA(lca_formula, data = encoded_data, nclass = n_classes, na.rm = TRUE)
#TEST
# Set seed for reproducibility
set.seed(123)
# Generate sample data: 5 binary variables (0, 1)
n_samples <- 1000
data <- data.frame(
var1 = sample(0:1, n_samples, replace = TRUE),
var2 = sample(0:1, n_samples, replace = TRUE),
var3 = sample(0:1, n_samples, replace = TRUE),
var4 = sample(0:1, n_samples, replace = TRUE),
var5 = sample(0:1, n_samples, replace = TRUE)
)
# Recode the data to start from 1
data <- data + 1
str(encoded_data)
str(data)
encoded_data[] <- lapply(encoded_data, as.factor)
str(encoded_data)
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("~", paste(colnames(encoded_data), collapse = " + ")))
n_classes <- 2
lca_result <- poLCA(lca_formula, data = encoded_data, nclass = n_classes, na.rm = TRUE)
encoded_data[] <- lapply(encoded_data, function(x) as.integer(as.factor(x)))
str(encoded_data)
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("~", paste(colnames(encoded_data), collapse = " + ")))
n_classes <- 2
lca_result <- poLCA(lca_formula, data = encoded_data, nclass = n_classes, na.rm = TRUE)
#lca_result <- poLCA(lca_formula, data = encoded_data, nclass = n_classes, na.rm = TRUE)
lca_result <- poLCA(lca_formula, data = encoded_data[1:1000, ], nclass = n_classes, na.rm = TRUE)
print(lca_formula)
anyNA(encoded_data)
sample_data <- encoded_data[1:100, c("Fatal", "Context_Other", "Context_FamilyConflict")]
lca_formula <- cbind(Fatal, Context_Other, Context_FamilyConflict) ~ 1
lca_result <- poLCA(lca_formula, data = sample_data, nclass = 2, na.rm = TRUE)
View(encoded_data)
library(dplyr)
library(here)
#install.packages("poLCA")
library(poLCA)
# Define the path to the CSV file
data_path <- normalizePath(file.path(here(), "..", "..", "data", "encoded", "encoded_data.csv"))
# Read the CSV file into a data frame
encoded_data <- read.csv(data_path, header = TRUE, sep = ",", stringsAsFactors = FALSE)
encoded_data[] <- lapply(encoded_data, function(x) {
if (all(x %in% c("True", "False"))) {  # Check if the column contains only "True" or "False"
return(as.integer(x == "True"))  # Convert "True" to 1, "False" to 0
} else {
return(x)  # Leave other columns unchanged
}
})
View(encoded_data)
subset_data <- encoded_data[encoded_data$GroupAge_19_34 == 1 & encoded_data$Gender == 1, ]
# Lista prefiksów kolumn do wybrania
selected_prefixes <- c('Income', 'Fatal', 'Method', 'Gender', 'Education', 'AbuseInfo',
'WorkInfo', 'Substance', 'Place', 'Marital')
# Wybieranie kolumn, których nazwy zaczynają się od dowolnego z prefiksów
selected_columns <- grep(paste0("^(", paste(selected_prefixes, collapse = "|"), ")"),
names(subset_data), value = TRUE)
# Tworzenie nowego zbioru danych zawierającego tylko wybrane kolumny
filtered_data <- subset_data[, selected_columns]
# Lista prefiksów kolumn do wybrania
selected_prefixes <- c('Income', 'Fatal', 'Method', 'Education', 'AbuseInfo',
'WorkInfo', 'Substance', 'Place', 'Marital')
# Wybieranie kolumn, których nazwy zaczynają się od dowolnego z prefiksów
selected_columns <- grep(paste0("^(", paste(selected_prefixes, collapse = "|"), ")"),
names(subset_data), value = TRUE)
# Tworzenie nowego zbioru danych zawierającego tylko wybrane kolumny
filtered_data <- subset_data[, selected_columns]
# Recode the data to start from 1
encoded_data <- encoded_data + 1
# Recode the data to start from 1
filtered_data <- filtered_data + 1
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("~", paste(colnames(filtered_data), collapse = " + ")))
n_classes <- 2
print(lca_formula)
anyNA(filtered_data)
# Print the result of the Latent Class Analysis
print(lca_result)
lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 2, na.rm = TRUE)
library(dplyr)
library(here)
#install.packages("poLCA")
library(poLCA)
# Define the path to the CSV file
data_path <- normalizePath(file.path(here(), "..", "..", "data", "encoded", "encoded_data.csv"))
# Read the CSV file into a data frame
encoded_data <- read.csv(data_path, header = TRUE, sep = ",", stringsAsFactors = FALSE)
encoded_data[] <- lapply(encoded_data, function(x) {
if (all(x %in% c("True", "False"))) {  # Check if the column contains only "True" or "False"
return(as.integer(x == "True"))  # Convert "True" to 1, "False" to 0
} else {
return(x)  # Leave other columns unchanged
}
})
subset_data <- encoded_data[encoded_data$GroupAge_19_34 == 1 & encoded_data$Gender == 1, ]
# Lista prefiksów kolumn do wybrania
selected_prefixes <- c('Income', 'Method', 'Education',
'WorkInfo', 'Substance', 'Place', 'Marital')
# Wybieranie kolumn, których nazwy zaczynają się od dowolnego z prefiksów
selected_columns <- grep(paste0("^(", paste(selected_prefixes, collapse = "|"), ")"),
names(subset_data), value = TRUE)
# Tworzenie nowego zbioru danych zawierającego tylko wybrane kolumny
filtered_data <- subset_data[, selected_columns]
# Recode the data to start from 1
filtered_data <- filtered_data + 1
#encoded_data[] <- lapply(encoded_data, as.factor)
#encoded_data[] <- lapply(encoded_data, function(x) as.integer(as.factor(x)))
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("~", paste(colnames(filtered_data), collapse = " + ")))
n_classes <- 2
lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 2, na.rm = TRUE)
lca_result <- poLCA(lca_formula, data = filtered_data[1:1000, ], nclass = 2, na.rm = TRUE)
View(encoded_data)
library(dplyr)
library(here)
#install.packages("poLCA")
library(poLCA)
# Define the path to the CSV file
data_path <- normalizePath(file.path(here(), "..", "..", "data", "encoded", "encoded_data.csv"))
# Read the CSV file into a data frame
encoded_data <- read.csv(data_path, header = TRUE, sep = ",", stringsAsFactors = FALSE)
encoded_data[] <- lapply(encoded_data, function(x) {
if (all(x %in% c("True", "False"))) {  # Check if the column contains only "True" or "False"
return(as.integer(x == "True"))  # Convert "True" to 1, "False" to 0
} else {
return(x)  # Leave other columns unchanged
}
})
subset_data <- encoded_data[encoded_data$GroupAge_19_34 == 1 & encoded_data$Gender == 1, ]
# Lista prefiksów kolumn do wybrania
selected_prefixes <- c('Income', 'Method', 'Education',
'WorkInfo', 'Substance', 'Place', 'Marital')
# Wybieranie kolumn, których nazwy zaczynają się od dowolnego z prefiksów
selected_columns <- grep(paste0("^(", paste(selected_prefixes, collapse = "|"), ")"),
names(subset_data), value = TRUE)
# Tworzenie nowego zbioru danych zawierającego tylko wybrane kolumny
filtered_data <- subset_data[, selected_columns]
# Recode the data to start from 1
filtered_data <- filtered_data + 1
#encoded_data[] <- lapply(encoded_data, as.factor)
#encoded_data[] <- lapply(encoded_data, function(x) as.integer(as.factor(x)))
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("~", paste(colnames(filtered_data), collapse = " + ")))
n_classes <- 2
lca_result <- poLCA(lca_formula, data = filtered_data[1:1000, ], nclass = 2, na.rm = TRUE)
View(filtered_data)
library(dplyr)
library(here)
#install.packages("poLCA")
library(poLCA)
# Define the path to the CSV file
data_path <- normalizePath(file.path(here(), "..", "..", "data", "encoded", "encoded_data.csv"))
# Read the CSV file into a data frame
encoded_data <- read.csv(data_path, header = TRUE, sep = ",", stringsAsFactors = FALSE)
encoded_data[] <- lapply(encoded_data, function(x) {
if (all(x %in% c("True", "False"))) {  # Check if the column contains only "True" or "False"
return(as.integer(x == "True"))  # Convert "True" to 1, "False" to 0
} else {
return(x)  # Leave other columns unchanged
}
})
subset_data <- encoded_data[encoded_data$GroupAge_19_34 == 1 & encoded_data$Gender == 1, ]
# Lista prefiksów kolumn do wybrania
#selected_prefixes <- c('Income', 'Method', 'Education',
#                       'WorkInfo', 'Substance', 'Place', 'Marital')
selected_prefixes <- c('Income', 'Method', 'Education')
# Wybieranie kolumn, których nazwy zaczynają się od dowolnego z prefiksów
selected_columns <- grep(paste0("^(", paste(selected_prefixes, collapse = "|"), ")"),
names(subset_data), value = TRUE)
# Tworzenie nowego zbioru danych zawierającego tylko wybrane kolumny
filtered_data <- subset_data[, selected_columns]
# Recode the data to start from 1
filtered_data <- filtered_data + 1
#encoded_data[] <- lapply(encoded_data, as.factor)
#encoded_data[] <- lapply(encoded_data, function(x) as.integer(as.factor(x)))
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("~", paste(colnames(filtered_data), collapse = " + ")))
n_classes <- 2
lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 2, na.rm = TRUE)
library(dplyr)
library(here)
#install.packages("poLCA")
library(poLCA)
# Define the path to the CSV file
data_path <- normalizePath(file.path(here(), "..", "..", "data", "encoded", "encoded_data.csv"))
# Read the CSV file into a data frame
encoded_data <- read.csv(data_path, header = TRUE, sep = ",", stringsAsFactors = FALSE)
encoded_data[] <- lapply(encoded_data, function(x) {
if (all(x %in% c("True", "False"))) {  # Check if the column contains only "True" or "False"
return(as.integer(x == "True"))  # Convert "True" to 1, "False" to 0
} else {
return(x)  # Leave other columns unchanged
}
})
subset_data <- encoded_data[encoded_data$GroupAge_19_34 == 1 & encoded_data$Gender == 1, ]
# Lista prefiksów kolumn do wybrania
#selected_prefixes <- c('Income', 'Method', 'Education',
#                       'WorkInfo', 'Substance', 'Place', 'Marital')
selected_prefixes <- c('Method', 'Education')
# Wybieranie kolumn, których nazwy zaczynają się od dowolnego z prefiksów
selected_columns <- grep(paste0("^(", paste(selected_prefixes, collapse = "|"), ")"),
names(subset_data), value = TRUE)
# Tworzenie nowego zbioru danych zawierającego tylko wybrane kolumny
filtered_data <- subset_data[, selected_columns]
# Recode the data to start from 1
filtered_data <- filtered_data + 1
#encoded_data[] <- lapply(encoded_data, as.factor)
#encoded_data[] <- lapply(encoded_data, function(x) as.integer(as.factor(x)))
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("~", paste(colnames(filtered_data), collapse = " + ")))
n_classes <- 2
lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 2, na.rm = TRUE)
lca_result <- poLCA(lca_formula, data = filtered_data[1:100, ], nclass = 2, na.rm = TRUE)
#TEST
# Set seed for reproducibility
set.seed(123)
# Generate sample data: 5 binary variables (0, 1)
n_samples <- 1000
data <- data.frame(
var1 = sample(0:1, n_samples, replace = TRUE),
var2 = sample(0:1, n_samples, replace = TRUE),
var3 = sample(0:1, n_samples, replace = TRUE),
var4 = sample(0:1, n_samples, replace = TRUE),
var5 = sample(0:1, n_samples, replace = TRUE)
)
# Recode the data to start from 1
data <- data + 1
str(data)
# Run Latent Class Analysis with 2 classes
lca_formula2 <- as.formula(cbind(var1, var2, var3, var4, var5) ~ 1)
lca_formula <- as.formula(cbind(colnames(filtered_data)) ~ 1)
lca_formula <- as.formula(paste("cbind(", paste(colnames(filtered_data), collapse = ", "), ") ~ 1"))
lca_result <- poLCA(lca_formula, data = filtered_data[1:100, ], nclass = 2, na.rm = TRUE)
#lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 2, na.rm = TRUE)
lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 2, na.rm = TRUE)
library(dplyr)
library(here)
#install.packages("poLCA")
library(poLCA)
# Define the path to the CSV file
data_path <- normalizePath(file.path(here(), "..", "..", "data", "encoded", "encoded_data.csv"))
# Read the CSV file into a data frame
encoded_data <- read.csv(data_path, header = TRUE, sep = ",", stringsAsFactors = FALSE)
encoded_data[] <- lapply(encoded_data, function(x) {
if (all(x %in% c("True", "False"))) {  # Check if the column contains only "True" or "False"
return(as.integer(x == "True"))  # Convert "True" to 1, "False" to 0
} else {
return(x)  # Leave other columns unchanged
}
})
subset_data <- encoded_data[encoded_data$GroupAge_19_34 == 1 & encoded_data$Gender == 1, ]
# Lista prefiksów kolumn do wybrania
selected_prefixes <- c('Income', 'Method', 'Education',
'WorkInfo', 'Substance', 'Place', 'Marital')
#selected_prefixes <- c('Method', 'Education')
# Wybieranie kolumn, których nazwy zaczynają się od dowolnego z prefiksów
selected_columns <- grep(paste0("^(", paste(selected_prefixes, collapse = "|"), ")"),
names(subset_data), value = TRUE)
# Tworzenie nowego zbioru danych zawierającego tylko wybrane kolumny
filtered_data <- subset_data[, selected_columns]
# Recode the data to start from 1
filtered_data <- filtered_data + 1
#encoded_data[] <- lapply(encoded_data, as.factor)
#encoded_data[] <- lapply(encoded_data, function(x) as.integer(as.factor(x)))
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("cbind(", paste(colnames(filtered_data), collapse = ", "), ") ~ 1"))
#lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 2, na.rm = TRUE)
lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 2, na.rm = TRUE)
# Print the result of the Latent Class Analysis
print(lca_result)
library(dplyr)
library(here)
#install.packages("poLCA")
library(poLCA)
# Define the path to the CSV file
data_path <- normalizePath(file.path(here(), "..", "..", "data", "encoded", "encoded_data.csv"))
# Read the CSV file into a data frame
encoded_data <- read.csv(data_path, header = TRUE, sep = ",", stringsAsFactors = FALSE)
encoded_data[] <- lapply(encoded_data, function(x) {
if (all(x %in% c("True", "False"))) {  # Check if the column contains only "True" or "False"
return(as.integer(x == "True"))  # Convert "True" to 1, "False" to 0
} else {
return(x)  # Leave other columns unchanged
}
})
subset_data <- encoded_data[encoded_data$GroupAge_19_34 == 1 & encoded_data$Gender == 1, ]
# Lista prefiksów kolumn do wybrania
selected_prefixes <- c('Fatal', 'Income', 'Method', 'Education',
'WorkInfo', 'Substance', 'Place', 'Marital')
#selected_prefixes <- c('Method', 'Education')
# Wybieranie kolumn, których nazwy zaczynają się od dowolnego z prefiksów
selected_columns <- grep(paste0("^(", paste(selected_prefixes, collapse = "|"), ")"),
names(subset_data), value = TRUE)
# Tworzenie nowego zbioru danych zawierającego tylko wybrane kolumny
filtered_data <- subset_data[, selected_columns]
# Recode the data to start from 1
filtered_data <- filtered_data + 1
#encoded_data[] <- lapply(encoded_data, as.factor)
#encoded_data[] <- lapply(encoded_data, function(x) as.integer(as.factor(x)))
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("cbind(", paste(colnames(filtered_data), collapse = ", "), ") ~ 1"))
#lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 2, na.rm = TRUE)
lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 2, na.rm = TRUE)
# Print the result of the Latent Class Analysis
print(lca_result)
library(dplyr)
library(here)
#install.packages("poLCA")
library(poLCA)
# Define the path to the CSV file
data_path <- normalizePath(file.path(here(), "..", "..", "data", "encoded", "encoded_data.csv"))
# Read the CSV file into a data frame
encoded_data <- read.csv(data_path, header = TRUE, sep = ",", stringsAsFactors = FALSE)
encoded_data[] <- lapply(encoded_data, function(x) {
if (all(x %in% c("True", "False"))) {  # Check if the column contains only "True" or "False"
return(as.integer(x == "True"))  # Convert "True" to 1, "False" to 0
} else {
return(x)  # Leave other columns unchanged
}
})
subset_data <- encoded_data[encoded_data$GroupAge_19_34 == 1 & encoded_data$Gender == 1, ]
# Lista prefiksów kolumn do wybrania
selected_prefixes <- c('Fatal', 'Income', 'Method', 'Education',
'WorkInfo', 'Substance', 'Place', 'Marital')
#selected_prefixes <- c('Method', 'Education')
# Wybieranie kolumn, których nazwy zaczynają się od dowolnego z prefiksów
selected_columns <- grep(paste0("^(", paste(selected_prefixes, collapse = "|"), ")"),
names(subset_data), value = TRUE)
# Tworzenie nowego zbioru danych zawierającego tylko wybrane kolumny
filtered_data <- subset_data[, selected_columns]
# Recode the data to start from 1
filtered_data <- filtered_data + 1
#encoded_data[] <- lapply(encoded_data, as.factor)
#encoded_data[] <- lapply(encoded_data, function(x) as.integer(as.factor(x)))
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("cbind(", paste(colnames(filtered_data), collapse = ", "), ") ~ 1"))
#lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 2, na.rm = TRUE)
lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 5, na.rm = TRUE)
# Print the result of the Latent Class Analysis
print(lca_result)
#TEST on sample
sample_data <- encoded_data[1:100, c("Fatal", "Context_Other", "Context_FamilyConflict")]
lca_formula <- cbind(Fatal, Context_Other, Context_FamilyConflict) ~ 1
lca_result <- poLCA(lca_formula, data = sample_data, nclass = 2, na.rm = TRUE)
library(dplyr)
library(here)
#install.packages("poLCA")
library(poLCA)
# Define the path to the CSV file
data_path <- normalizePath(file.path(here(), "..", "..", "data", "encoded", "encoded_data.csv"))
# Read the CSV file into a data frame
encoded_data <- read.csv(data_path, header = TRUE, sep = ",", stringsAsFactors = FALSE)
encoded_data[] <- lapply(encoded_data, function(x) {
if (all(x %in% c("True", "False"))) {  # Check if the column contains only "True" or "False"
return(as.integer(x == "True"))  # Convert "True" to 1, "False" to 0
} else {
return(x)  # Leave other columns unchanged
}
})
subset_data <- encoded_data
# Lista prefiksów kolumn do wybrania
selected_prefixes <- c('Fatal', 'Income', 'Method', 'Education',
'WorkInfo', 'Substance', 'Place', 'Marital', 'Context', 'Gender', 'GroupAge')
#selected_prefixes <- c('Method', 'Education')
# Wybieranie kolumn, których nazwy zaczynają się od dowolnego z prefiksów
selected_columns <- grep(paste0("^(", paste(selected_prefixes, collapse = "|"), ")"),
names(subset_data), value = TRUE)
# Tworzenie nowego zbioru danych zawierającego tylko wybrane kolumny
filtered_data <- subset_data[, selected_columns]
# Recode the data to start from 1
filtered_data <- filtered_data + 1
#encoded_data[] <- lapply(encoded_data, as.factor)
#encoded_data[] <- lapply(encoded_data, function(x) as.integer(as.factor(x)))
# Perform Latent Class Analysis
lca_formula <- as.formula(paste("cbind(", paste(colnames(filtered_data), collapse = ", "), ") ~ 1"))
#lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 2, na.rm = TRUE)
lca_result <- poLCA(lca_formula, data = filtered_data, nclass = 3, na.rm = TRUE)
# Print the result of the Latent Class Analysis
print(lca_result)
